export enum Path {
  authPage = 'auth',
  signupPage = 'signup',
  signinPage = 'signin',
  loginPage = 'login',
}

export enum StorageKeys {
  authToken = 'auth-token',
  user = 'user',
}

export const defaultUserName = 'Your Name';

export enum Path {
  homePage = 'home',
  authPage = 'auth',
  signupPage = 'signup',
  loginPage = 'login',
}

export enum StorageKeys {
  authToken = 'auth-token-rstrello',
  user = 'user-rstrello',
}

export enum Path {
  authPage = 'auth',
  signupPage = 'signup',
  signinPage = 'sigin',
  loginPage = 'login',
}

export enum StorageKeys {
  authToken = 'auth-token',
  user = 'user',
}

export const defaultUserName = 'Your Name';
